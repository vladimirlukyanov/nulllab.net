---
import Head from "~/components/head/index.astro";
import { SITE_TITLE, SITE_DESCRIPTION, SITE_KEYWORDS } from "../consts";

// Components
import Header from "~/components/header/index.astro";
import Footer from "~/components/footer/index.astro";
const { title, description, keywords, lang = 'en', noindex, nofollow } = Astro.props;

---

<!doctype html>
<html lang={lang} data-theme="dark">
  <head>
    <Head
        title={SITE_TITLE + ' – Software development, research & tech experiments'}
        description={SITE_DESCRIPTION}
        keywords={SITE_KEYWORDS}
    >
      <meta name="robots" content="index, follow, max-snippet:-1, max-video-preview:-1" />
    </Head>
  </head>
  <body itemscope itemtype="http://schema.org/WebPage" class="preload">
    <Header />
    <main class="main">
      <slot />
    </main>
    <Footer />
  </body>
</html>

<style lang="scss">
  @use "sass:list";
  @use "sass:string";
  @use "sass:meta";
  @use "sass:map";

  @use "~/styles/app.scss" as app;

  @use "~/styles/scss/mixins.scss" as mixins;
  @use "~/styles/scss/functions.scss" as functions;

  $main-section: (
      margin: 0 0 15px 0,
      padding-inline: var(--padding-inline),
  );

  @include mixins.generate-component(
      $main-section,
      "main",
      app.$config,
      app.$theme
  );
</style>

